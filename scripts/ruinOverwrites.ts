// These overwrites are needed since
// the source does not contain accurate data

import { Ruin } from "./generateData";

export const overwriteRuinData = (_ruins: Record<number, Ruin>) => {
  // Nothing here anymore, the drops were updated in the main repo
};
